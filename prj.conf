# Zephyr feature switches to turn components on and off
# GPIO is still needed by the board support layer
CONFIG_GPIO=y

# Enable the PWM driver so LEDs can be dimmed via duty-cycle control
CONFIG_PWM=y

# Enable C++ support
CONFIG_CPP=y
CONFIG_STD_CPP17=y
